<Window x:Class="KiCadExcelBridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KiCadExcelBridge"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="KiCad Database Library Configuration" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Source Excel/CSV Files" Grid.Column="0" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox x:Name="SourceFilesList" Grid.Row="0"/>
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <Button Content="+ Add File..." Margin="5" Click="AddFile_Click"/>
                    <Button Content="- Remove File" Margin="5" Click="RemoveFile_Click"/>
                    <Button Content="⟳ Refresh" Margin="5" Click="RefreshFiles_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="Sheet to Category Mapping" Grid.Column="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="SheetMappingsGrid" Grid.Row="0" AutoGenerateColumns="False" CanUserDeleteRows="False" PreviewKeyDown="SheetMappingsGrid_PreviewKeyDown">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Source File" Binding="{Binding SourceFile}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Sheet Name" Binding="{Binding SheetName}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Category Label" Binding="{Binding CategoryLabel}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Ignore First Row" Binding="{Binding IgnoreHeader}" Width="140"/>
                        <DataGridTemplateColumn>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="..." Click="ConfigureFields_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <!-- Per-sheet "Ignore first row" is controlled per-row in the DataGrid; global checkbox removed -->
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="OK" Margin="5" Click="Ok_Click"/>
            <Button Content="Cancel" Margin="5" Click="Cancel_Click"/>
            <Button Content="Apply" Margin="5" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>
